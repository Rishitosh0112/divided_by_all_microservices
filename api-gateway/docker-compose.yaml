version: "3.9"

services:
  api-gateway:
    build:
      context: .
    container_name: api-gateway
    ports:
      - "8080:80"
    networks:
      - devtinder-net

networks:
  devtinder-net:
    external: true
